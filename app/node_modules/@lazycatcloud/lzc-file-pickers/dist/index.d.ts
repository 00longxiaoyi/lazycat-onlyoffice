/**
 * 懒猫文件选择器类型声明文件
 * 自动生成于: 2026-04-01T06:15:28.106Z
 */

export enum filePickerType {
  file = "file",
  directory = "directory",
}
export enum tableListType {
  collect = "collect",
  networkDir = "networkDir",
  externalHardDrive = "externalHardDrive",
  appData = "appData",
  fileMount = "fileMount",
}
export interface filePickerPropsType {
  /**
   * @deprecated
   * 
   * 内部自行判断不需要传递
   */
  boxId?: string;
  type: filePickerType;
  title?: string;
  rootpath?: string;
  rootname?: string;
  source?: string[]; // 底部名称字段透传（undefined不显示，[]禁用确认）
  accept?: string; //过滤
  extname?: string; //过滤
  filterSelectVisible?: boolean; // 是否显示扩展名过滤切换下拉
  confirmButtonTitle?: string; //确认标题
  tableList?: tableListType[]; //显示哪些table
  multiple?: boolean; //是否多选
  isModal?: boolean //是否是弹窗
  choiceFileOnly?: boolean //是否只选择文件
  choiceDirOnly?: boolean //是否只选择文件夹
  ignorePath?: string[] //过滤路径
}


// Vue组件类型声明
import { DefineComponent } from 'vue'

/**
 * 文件选择器组件类型
 */
export declare const FilePicker: DefineComponent<filePickerPropsType>

/**
 * 自定义元素声明
 * 支持在HTML中直接使用 <lzc-file-picker> 标签
 */
declare global {
  namespace JSX {
    interface IntrinsicElements {
      'lzc-file-picker': filePickerPropsType
    }
  }
  
  interface HTMLElementTagNameMap {
    'lzc-file-picker': HTMLElement
  }
}
